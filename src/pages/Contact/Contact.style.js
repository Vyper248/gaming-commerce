import styled from 'styled-components';

const StyledContact = styled.div`
    & > form {
        max-width: 700px;
        margin: auto;
    }

    & > div {
        max-width: 700px;
        margin: auto;
        margin-top: 20px;
    }
`

export default StyledContact;